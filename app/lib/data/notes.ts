import { Note } from '../types';

export const premiumNotes: Note[] = [
  {
    id: 'p1',
    title: 'Prompt vs Programmatic Enforcement',
    category: 'Core Patterns',
    pattern: 'P1',
    priority: 'critical',
    content: 'The single most tested concept. Any policy that MUST be enforced — financial thresholds, tool restrictions, output schemas — belongs in code (hooks, schemas, tool_choice), never in prose instructions alone.',
    keyPoints: [
      'PreToolUse hooks can deny tool calls before they execute — this is the correct place for hard limits like "refund > $500 requires approval token"',
      'tool_choice: "any" or strict: true provide programmatic guarantee of structured output vs prompt-only requests',
      'System prompt instructions are probabilistic; they will be violated in edge cases at scale',
      'Regex, schema validators, and programmatic gates are deterministic — use them for hard requirements',
    ],
    interviewTip: 'If a question describes a critical policy (financial, security, compliance) being enforced via system prompt and asks why it fails — always suspect P1. The fix is always programmatic.',
  },
  {
    id: 'p2',
    title: 'First Step Proportionality',
    category: 'Core Patterns',
    pattern: 'P2',
    priority: 'high',
    content: 'Match the complexity of the approach to the complexity of the task. Simple single-file edits do not need plan mode. Complex multi-file refactors with uncertain scope absolutely do.',
    keyPoints: [
      'Plan mode (Explore → Plan → Implement → Commit) is for: multi-file changes, unfamiliar codebase, architecturally uncertain scope',
      'Skip plan mode for: typo fixes, single-sentence diffs, well-understood simple changes',
      'The heuristic: "if you could describe the diff in one sentence, skip the plan"',
      'Batching independent corrections in one prompt is efficient; sequential micro-corrections waste turns',
    ],
    interviewTip: 'Watch for questions where a developer uses plan mode for a one-line change or skips it for a 17-file refactor. Both are wrong; match the approach to complexity.',
  },
  {
    id: 'p3',
    title: 'Over-Engineering Distractor',
    category: 'Core Patterns',
    pattern: 'P3',
    priority: 'high',
    content: 'Exam traps include: adding a classifier agent when better tool descriptions would fix selection, spawning subagents when a single Edit would do, or using consensus review when one specialized agent would suffice.',
    keyPoints: [
      'Tool description quality is almost always the cheapest first fix — not routing agents or classifiers',
      'Skills/subagents add overhead; use them when tasks genuinely require isolation or parallelism',
      'Regex fallbacks for structured output = wrong layer; tool use with strict schemas is the right fix',
      'A second Claude instance reviewing the first is P19 (Self-Review Limitation) if using the same prompt',
    ],
    interviewTip: 'When multiple answer choices include "add another agent/classifier/routing step," first ask if the simpler fix (better description, nullable field, schema constraint) isn\'t the answer.',
  },
  {
    id: 'p5',
    title: 'Tool Description Primacy',
    category: 'Tool Design',
    pattern: 'P5',
    priority: 'critical',
    content: 'Anthropic explicitly states tool descriptions are "by far the most important factor in tool performance." A one-line description almost always produces routing errors or fabrication; 3-4+ sentences are the documented minimum.',
    keyPoints: [
      'Each tool description should cover: what the tool does, when to use it, when NOT to use it, what each parameter means, important caveats',
      '"Extracts diagnosis codes from clinical notes" → too short. Needs: scope, exclusions, null-return policy',
      'Tool descriptions drive Claude\'s semantic discovery — they are what it uses to disambiguate between similar tools',
      'Front-load the most important constraint: "Use ONLY when the user asks to EXECUTE a migration" not "runs migrations"',
    ],
    interviewTip: 'If a tool is being confused with another or is producing fabricated outputs, the first diagnostic is always: is the description long enough and explicit enough?',
  },
  {
    id: 'p6',
    title: 'Context Inheritance Misconception',
    category: 'Multi-Agent',
    pattern: 'P6',
    priority: 'critical',
    content: 'Subagents do NOT inherit the coordinator\'s conversation history. They start with a blank context. Every fact the subagent needs must be explicitly passed in the subagent\'s prompt string.',
    keyPoints: [
      'The only channel from parent to subagent is the Agent tool\'s prompt string',
      'A subagent that "knows" facts from the coordinator is either hallucinating or received them in its prompt',
      'Messages API is fully stateless — every request must include the full prior conversation in the messages array',
      'Prompt caching reduces cost but does NOT make the API stateful — tokens must still be sent every time',
    ],
    interviewTip: 'Whenever a subagent produces results that reference information the coordinator had but never passed — the answer is P6. The fix is to serialize that data into the subagent\'s prompt.',
  },
  {
    id: 'p7',
    title: 'Batch API Latency Trap',
    category: 'Infrastructure',
    pattern: 'P7',
    priority: 'high',
    content: 'The Batch API offers 50% cost savings but has up to 24-hour processing time with NO guaranteed SLA. Never use it for blocking, user-facing, or time-sensitive workflows.',
    keyPoints: [
      'Worst-case end-to-end latency = submit_interval + 24h processing ceiling',
      'For a 30h SLA: 4h submit cadence = 28h worst case (2h headroom) ✓',
      'No in-flight prompt update endpoint exists — fix prompts BEFORE batch submission via synchronous pilot',
      'Only succeeded requests are billed; errored/expired/canceled are not charged',
      'The Batch API has no PATCH endpoint — cancel and resubmit is the only correction path',
    ],
    interviewTip: 'If a question proposes using Batch API for a pre-merge CI check or a real-time support agent — that\'s the trap. If it proposes patching an in-flight batch — also a trap (no such endpoint).',
  },
  {
    id: 'p10',
    title: 'Root Cause vs Symptom',
    category: 'Core Patterns',
    pattern: 'P10',
    priority: 'critical',
    content: 'Exam questions consistently present a symptom (wrong tool called, fabricated data, low accuracy) and tempt you with symptom-level fixes (retry, bigger model, prompt sentiment). Always diagnose the structural root cause.',
    keyPoints: [
      'stop_reason: "end_turn" is not a bug — it means the model naturally stopped. The bug is elsewhere (tool description, prompt logic)',
      'Fabricated required fields → root cause is schema design (make it nullable), not retries',
      'Inconsistent CI output → root cause is auto-discovered config variance, not temperature',
      'Low adherence to CLAUDE.md rules → root cause is file too long (>200 lines), not model capability',
    ],
    interviewTip: 'Whenever you see "add more retries," "upgrade the model," or "increase temperature" — these are almost always distractors. Ask: what structural condition is forcing this bad behavior?',
  },
  {
    id: 'p11',
    title: 'Scoped Access / Least Privilege',
    category: 'Security',
    pattern: 'P11',
    priority: 'high',
    content: 'Each subagent or tool should have access only to what it actually needs. Loading 34 tools into every subagent degrades selection accuracy and increases attack surface.',
    keyPoints: [
      'Per-subagent tools allowlists limit tool surface to the role\'s actual needs',
      'The "Tool Search Tool" pattern exists because loading many definitions upfront hurts performance',
      'allowed-tools in SKILL.md pre-approves listed tools — it does NOT restrict other tools',
      'Account disambiguation must happen before any account data is accessed — never act on ambiguous identity',
    ],
    interviewTip: 'If a subagent has access to 30+ tools and tool selection accuracy is dropping — the fix is always scoping, not larger context windows.',
  },
  {
    id: 'p14',
    title: 'Nullable Field Prevention',
    category: 'Schema Design',
    pattern: 'P14',
    priority: 'high',
    content: 'When a field is required in the schema but the source document may not contain the data, the model is structurally forced to fabricate. The fix is nullable typing + explicit "return null if absent" description.',
    keyPoints: [
      'Pattern: change type from "number" to ["number", "null"] and keep it in required',
      'Description must say: "Return null if no confirmed value appears in the source. Do not estimate or convert informal measurements."',
      'The "enum + other + detail" pattern: add "other" to enum + a nullable sibling detail field for the ~6% edge cases',
      'Optional fields (not in required) and nullable fields solve different problems — nullable is usually correct',
    ],
    interviewTip: 'Dosage, tax ID, hazmat class, customs value — whenever a sparse or conditional field is causing fabrication, the answer involves nullable typing, never more retries.',
  },
  {
    id: 'p15',
    title: 'Configuration Hierarchy',
    category: 'Claude Code',
    pattern: 'P15',
    priority: 'critical',
    content: 'Claude Code has a strict 4-level config hierarchy: Managed (enterprise) → Project (.claude/) → User (~/.claude/) → Local (.claude/settings.local.json). Understanding what gets shared vs what stays personal is frequently tested.',
    keyPoints: [
      '.mcp.json at project root → checked into git, shared with team, requires user approval before first use',
      '~/.claude/agents/ → user-level, personal, NOT shared via git',
      '.claude/agents/ → project-level, checked into git, shared with team',
      '.claude/settings.local.json → git-ignored by default (personal per-checkout overrides)',
      'Rules with paths: frontmatter → lazy-loaded only when matching file is read',
      'Rules without paths: → loaded unconditionally at session start',
    ],
    interviewTip: 'Questions asking "where should X live to be shared with all teammates" → always .mcp.json or .claude/ (project level). "For just one developer" → ~/.claude/ (user level).',
  },
  {
    id: 'p16',
    title: 'Progressive Summarization Risk',
    category: 'Context Management',
    pattern: 'P16',
    priority: 'high',
    content: 'Summarization compresses toward semantic gist and loses transactional specifics: exact amounts, account numbers, dates, explicit commitments. These must be extracted into persistent pinned blocks BEFORE summarization occurs.',
    keyPoints: [
      'Root CLAUDE.md is re-read from disk and re-injected after /compact — it survives',
      'Nested subdirectory CLAUDE.md files are summarized away — they reload only on next matching file read',
      '/compact accepts custom instructions to guide what gets preserved: "Focus on API endpoints modified, new request schemas, remaining TODOs"',
      'Transactional facts (refund amounts, card last-4, promised timelines) must bypass the summary layer entirely',
    ],
    interviewTip: 'If a question describes a customer service agent "forgetting" specific dollar amounts or account numbers after many turns — this is P16. The fix is a persistent pinned facts block.',
  },
  {
    id: 'p17',
    title: 'Lost in the Middle',
    category: 'Context Management',
    pattern: 'P17',
    priority: 'high',
    content: 'Models reliably attend to the beginning and end of long inputs but degrade in the middle. For long CLAUDE.md files, critical rules buried at line 620 get ignored. For multi-source synthesis, middle reports get omitted.',
    keyPoints: [
      'CLAUDE.md adherence degrades when the file exceeds ~200 lines — rules buried in the middle get missed',
      'Fix for multi-source synthesis: hoist compact "key findings" headers to the TOP of the aggregated input',
      'Fix for long diffs: split into per-file agents, each with a small focused input — no content in the middle',
      'Larger context windows make lost-in-the-middle WORSE, not better, by enlarging the under-attended region',
    ],
    interviewTip: 'Watch for: "Claude correctly reviews the start and end of a 800-line diff but misses issues in the middle." Architecture fix (per-file agents) beats prompt instructions every time.',
  },
  {
    id: 'p18',
    title: 'Fresh Session vs Resume',
    category: 'Claude Code',
    pattern: 'P18',
    priority: 'high',
    content: 'Fresh sessions for: CI/CD (every run isolated), new PR reviews, TDD (after >2 failed corrections), new features after spec-writing. Resume sessions for: continuing the same work with still-valid context.',
    keyPoints: [
      '--continue (-c) → drops into the most recent session non-interactively (no picker)',
      '--resume (-r) → opens interactive picker OR accepts an explicit session ID',
      '-p sessions don\'t appear in the picker but CAN be resumed by passing captured session_id explicitly',
      'Session forking: /branch or --resume <id> --fork-session creates a branch with its own session ID',
      'Commit failing tests as checkpoint before starting fresh session for TDD',
    ],
    interviewTip: 'The exam conflates --continue and --resume. Remember: --continue = most recent, no picker. --resume = interactive picker or explicit ID.',
  },
  {
    id: 'p19',
    title: 'Self-Review Limitation',
    category: 'Multi-Agent',
    pattern: 'P19',
    priority: 'high',
    content: 'A model in the same conversation is biased toward its own prior reasoning and defends rather than challenges it. Independent review requires: fresh API invocation, no prior assistant turns, ideally different system prompt or model.',
    keyPoints: [
      '"Now critically review your findings" in the same conversation → 3% reversal rate at best (just sycophantic editing)',
      'True adversarial review: fresh API invocation with ONLY the findings + source as input, scoped to disprove',
      'Extended thinking improves single-pass depth but still operates within the same instance — not a substitute',
      'N identical agents with the same prompt produce CORRELATED errors, not independent confirmation',
    ],
    interviewTip: 'Any question where a second review pass happens in the same conversation → P19. The fix always involves a fresh invocation with no inherited context.',
  },
  {
    id: 'p20',
    title: 'Explicit Escalation Criteria',
    category: 'Agent Design',
    pattern: 'P20',
    priority: 'critical',
    content: 'Vague escalation triggers ("when the user is frustrated," "when the issue is complex") produce inconsistent behavior. Replace with explicit, observable, testable conditions that can be verified by inspection.',
    keyPoints: [
      'Good explicit trigger: "Escalate when ANY of: (1) customer uses refund/cancel/lawsuit keyword, (2) issue reopened ≥2 times, (3) resolution requires actions the tools cannot perform, (4) customer explicitly requests human"',
      'Sentiment score thresholds are P4 (Sentiment/Confidence Trap) — they correlate with tone, not resolvability',
      'Explicit human escalation request is a HARD trigger — must be honored even if the agent could resolve the issue',
      'Policy gaps (no policy coverage for a scenario) must also be explicit escalation criteria',
    ],
    interviewTip: 'Sentiment-based escalation → P4/P20 combo trap. The fix replaces the sentiment threshold with observable business conditions.',
  },
];

export const noteCategories = [...new Set(premiumNotes.map(n => n.category))];
